interface:
  display_name: "Clean Code"
  short_description: "Polish changed code without changing behavior"
  default_prompt: "Use $clean-code to review and polish the code changed in this task while preserving behavior."
